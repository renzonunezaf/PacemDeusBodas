<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:name=".PacemDeusApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key"/>

        <activity android:name=".ui.SplashActivity" android:exported="true" android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity"/>
        <activity android:name=".ui.auth.RegisterActivity"/>

        <!-- Couple: novios -->
        <activity android:name=".ui.couple.CoupleHomeActivity"/>
        <activity android:name=".ui.couple.CreateEditWeddingActivity"/>
        <activity android:name=".ui.couple.AssemblyActivity"/>
        <activity android:name=".ui.couple.InstrumentsActivity"/>
        <activity android:name=".ui.couple.SetlistActivity"/>
        <activity android:name=".ui.couple.ContractActivity"/>

        <!-- Coordinador general (ADMIN) -->
        <activity android:name=".ui.coordinator.CoordinatorHomeActivity"/>
        <activity android:name=".ui.coordinator.MapScreenActivity"/>
        <activity android:name=".ui.coordinator.ApproveActivity"/>
        <activity android:name=".ui.coordinator.WeddingDetailActivity"/>
        <activity android:name=".ui.coordinator.CameraActivity"/>
        <activity android:name=".ui.coordinator.AssignPlannerActivity"/>

        <!-- Wedding planner -->
        <activity android:name=".ui.planner.PlannerDashboardActivity"/>
        <activity android:name=".ui.planner.PlannerDetailActivity"/>
    </application>
</manifest>
