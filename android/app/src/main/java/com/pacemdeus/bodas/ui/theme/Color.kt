package com.pacemdeus.bodas.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta oficial Pacem Deus Bodas. Inspirada en el sitio web del coro
// (dorado + crema + marron). Usar via referencias directas (Gold, Brown,
// Cream, etc.) para mantener consistencia visual sin pelearnos con el
// sistema de roles de Material3.

val Gold       = Color(0xFFB8995E)
val GoldDark   = Color(0xFF8C7344)
val GoldSoft   = Color(0xFFEFE5D0)
val Brown      = Color(0xFF3E2F1C)
val BrownLight = Color(0xFF6B5A41)
val Cream      = Color(0xFFFAF6EE)
val Sand       = Color(0xFF9B8A6E)
val Divider    = Color(0xFFE5DDC9)
val NavBg      = Color(0xFFFFFFFF)

// Colores semanticos para estados
val Success    = Color(0xFF2E7D32)
val Danger     = Color(0xFFC62828)
val Warning    = Color(0xFFEF6C00)
val Info       = Color(0xFF1565C0)
